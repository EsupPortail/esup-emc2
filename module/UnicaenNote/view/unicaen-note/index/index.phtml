<?php

/**
 * @see \UnicaenNote\Controller\IndexController::indexAction()
 */

use UnicaenNote\Provider\Privilege\NotePrivileges;
use UnicaenNote\Provider\Privilege\PortenotePrivileges;
use UnicaenNote\Provider\Privilege\TypePrivileges;

$this->headTitle("Index du module UnicaenNote");

$canPortenote = $this->isAllowed(PortenotePrivileges::getResourceId(PortenotePrivileges::PORTENOTE_INDEX));
$canType = $this->isAllowed(TypePrivileges::getResourceId(TypePrivileges::TYPE_INDEX));
$canNote = $this->isAllowed(NotePrivileges::getResourceId(NotePrivileges::NOTE_INDEX));

?>

<h1 class="page-header">
    Index du module UnicaenNote
</h1>

<div class="main">

    <?php if ($canPortenote) : ?>
        <h2> Gestion des porte-notes</h2>

        <p> Les <strong>porte-note</strong> sont des structures permettant de regrouper plusieurs notes et de gérer leurs affichages et gestions.</p>
        <a href="<?php echo $this->url('unicaen-note/porte-note', [], [], true); ?>" class="btn btn-primary">
            Index des porte-notes
        </a>
    <?php endif; ?>

    <?php if ($canType) : ?>
        <h2> Gestion des types de notes</h2>

        <p> Les notes peuveunt être typées ...</p>
        <a href="<?php echo $this->url('unicaen-note/type', [], [], true); ?>" class="btn btn-primary">
            Index des types de notes
        </a>
    <?php endif; ?>

    <?php if ($canNote) : ?>
        <h2> Gestion des notes</h2>

        <p> Gestion des notes ...</p>
        <a href="<?php echo $this->url('unicaen-note/note', [], [], true); ?>" class="btn btn-primary">
            Index des notes
        </a>
    <?php endif; ?>
</div>
