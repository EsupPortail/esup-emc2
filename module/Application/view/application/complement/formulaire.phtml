<?php

use Application\Entity\Db\Complement;
use Application\Form\Complement\ComplementForm;

/**
 * @see \Application\Controller\ComplementController::ajouterAction()
 * @see \Application\Controller\ComplementController::modifierAction()
 * @var string $title
 * @var ComplementForm $form
 * @var string $type
 */

?>

<div class="alert alert-info">
    <?php if ($type === Complement::COMPLEMENT_TYPE_AUTORITE) : ?>
        <p> L'Autorité hiérarchique est le responsable de la structure de plus haut niveau de l'agent (p.e. DSI, UFR LVE, ...). </p>
        <p> L'Autorité hiérarchique validera l'entretien professionnel de l'Agent. </p>
    <?php endif; ?>
    <?php if ($type === Complement::COMPLEMENT_TYPE_RESPONSABLE) : ?>
        <p> Le supérieur hiérarchique direct est le responsable de la structure de l'agent (p.e. Pôle développement, DS GREYC, ...). </p>
        <p> Le supérieur hiérarchique direct est la personne qui fera l'entretien professionnel de l'Agent. </p>
    <?php endif; ?>
</div>

<?php echo $this->form($form); ?>
