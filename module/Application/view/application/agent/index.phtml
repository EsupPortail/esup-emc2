<?php

/**
 * @see \Application\Controller\AgentController::indexAction()
 * @var array $agents ['id' => string, 'prenom' => string, 'nom_usage' => string, 'utilisateur' => string ]
 */

use Application\Entity\Db\Agent;
use Application\Provider\Privilege\AgentPrivileges;

$canAfficher = $this->isAllowed(AgentPrivileges::getResourceId(AgentPrivileges::AGENT_AFFICHER));

$this->headTitle("Index des agents")
?>

<div class="row">
    <div class="col-md-6">
    <h1 class="page-header">
        Index des agents
        <span class="badge">
               <?php echo count($agents); ?>
           </span>
    </h1>
    </div>
    <div class="pull-right col-md-5">
        <br/>
        <div class=" alert alert-info">
            <span class="icon icon-synchroniser"></span>
            Les agents sont synchronisés depuis Octopus.
        </div>
    </div>
</div>

<div class="main">

    <?php if (count($agents) > 0) : ?>
        <table id="agents" class="datatable table table-condensed">
            <thead>
            <tr>
                <th> Source Id </th>
                <th> Dénomination </th>
                <th> <span title="Lié à un utilisateur de l'application">Lié</span> </th>
                <th class="action"> Action </th>
            </tr>
            </thead>
            <tbody>
            <?php foreach ($agents as $agent) : ?>
                <tr>
                    <td> <span class="badge">Octopus - <?php echo $agent['id']; ?></span></td>
                    <td> <?php echo $agent['prenom']; ?> <?php echo $agent['nom_usage']; ?> </td>
                    <td>
                        <?php if ($agent['utilisateur'] !== null) : ?>
                            <span class="icon icon-checked" style="color:darkgreen;" title="<?php echo $agent['utilisateur']; ?>"></span>
                        <?php else: ?>
                            <span class="icon icon-retirer" style="color:darkred;"></span>
                        <?php endif; ?>
                    </td>
                    <td>
                        <?php if ($canAfficher) : ?>
                            <a
                                <?php /** @see \Application\Controller\AgentController::afficherAction() */?>
                                href="<?php echo $this->url('agent/afficher', ['agent' => $agent['id']], [], true); ?>"
                            >
                                <span class="icon icon-voir"  title="Afficher l'agent"></span></a>
                        <?php endif; ?>
                    </td>
                </tr>
            <?php endforeach; ?>
            </tbody>
        </table>
    <?php endif; ?>

</div>

<script>
    $(document).ready(function() {
        $('#agents').DataTable( {
            "sorting":   false,
            language: {
                url: '/localisation/fr_FR.json'
            }
        } );
    } );
</script>

