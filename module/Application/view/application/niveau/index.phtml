<?php

use Application\Entity\Db\Niveau;

/**
 * @see \Application\Controller\NiveauController::indexAction()
 * @var Niveau[] $niveaux
 */

$this->headTitle('Index des niveaux');

$canAjouterNiveau = true;
$canModifierNiveau = true;
$canHistoriserNiveau = true;
$canSupprimerNiveau = true;

?>

<div class="row">
    <div class="col-md-6">
        <h1 class="page-header">
            Index des niveaux
        </h1>
    </div>
    <div class="pull-right">
        <?php if ($canAjouterNiveau) : ?>
            <?php /** @see \Application\Controller\NiveauController::ajouterAction() */ ?>
            <a href="<?php echo $this->url('niveau/ajouter', [], [], true); ?>"
               class="ajax-modal btn btn-primary" data-event="modification"
            >
                <span class="icon ajouter"></span>
                Ajouter un niveau
            </a>
        <?php endif; ?>
    </div>
</div>

<table class="table table-condensed">
    <thead>
        <tr>
            <th> Niveau </th>
            <th> Étiquette </th>
            <th> Libelle </th>
            <th> Action </th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($niveaux as $niveau) : ?>
            <tr>
                <td> <?php echo $niveau->getNiveau(); ?> </td>
                <td> <?php echo $niveau->getEtiquette(); ?> </td>
                <td>
                    <?php echo $niveau->getLibelle(); ?>
                    <?php if ($niveau->getDescription()) : ?>
                        <span class="icon information" title="<?php echo $niveau->getDescription(); ?> "
                              data-toggle="tooltip" data-html="true";
                        ></span>
                    <?php endif; ?>
                </td>
                <td>
                    <?php if ($canModifierNiveau) : ?>
                        <?php /** @see \Application\Controller\NiveauController::modifierAction() */?>
                        <a href="<?php echo $this->url('niveau/modifier', ['niveau' => $niveau->getId()], [], true); ?>"
                           class="ajax-modal" data-event="modification"
                           data-toogle="tooltip" data-html="true" title="Modifier le niveau <?php echo $niveau->getEtiquette(); ?>"
                        >
                            <span class="icon editer"></span></a>
                    <?php endif; ?>
                    <?php if ($canHistoriserNiveau) : ?>
                        <?php if($niveau->estNonHistorise()) : ?>
                            <?php /** @see \Application\Controller\NiveauController::historiserAction() */?>
                            <a href="<?php echo $this->url('niveau/historiser', ['niveau' => $niveau->getId()], [], true); ?>"
                               data-toogle="tooltip" data-html="true" title="Historiser le niveau <?php echo $niveau->getEtiquette(); ?>"
                            >
                                <span class="icon historiser"></span></a>
                        <?php else : ?>
                            <?php /** @see \Application\Controller\NiveauController::restaurerAction() */?>
                            <a href="<?php echo $this->url('niveau/restaurer', ['niveau' => $niveau->getId()], [], true); ?>"
                               data-toogle="tooltip" data-html="true" title="Rstaurer le niveau <?php echo $niveau->getEtiquette(); ?>"
                            >
                                <span class="icon restaurer"></span></a>
                        <?php endif; ?>
                    <?php endif; ?>
                    <?php if ($canSupprimerNiveau) : ?>
                        <?php /** @see \Application\Controller\NiveauController::supprimerAction() */?>
                        <a href="<?php echo $this->url('niveau/supprimer', ['niveau' => $niveau->getId()], [], true); ?>"
                           class="ajax-modal" data-event="modification"
                           data-toogle="tooltip" data-html="true" title="Supprimer le niveau <?php echo $niveau->getEtiquette(); ?>"
                        >
                            <span class="icon detruire"></span></a>
                    <?php endif; ?>
                </td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>

<script>
    $(function() {
        $("body").on("modification", function (event) {
            event.div.modal('hide');
            window.location.reload();
        });
    });
</script>