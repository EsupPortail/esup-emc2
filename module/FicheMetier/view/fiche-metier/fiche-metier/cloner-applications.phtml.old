<?php

/**
 * @see \Application\Controller\FicheMetierController::clonerApplicationsAction()
 * @var FicheMetier $ficheMetier
 * @var SelectionFicheMetierForm $form
 */

use FicheMetier\Entity\Db\FicheMetier;
use Application\Form\SelectionFicheMetier\SelectionFicheMetierForm;

?>

<?php echo $this->form($form); ?>

<div id="affichage-applications">
    Zone d'affichage des applications ...
</div>


<script>
    $('#fiche-metier').change(function() {
        let fichemetier_id = $("#fiche-metier").val();

        let url = "../afficher-applications/" + fichemetier_id;
        $.ajax({
            type: "POST",
            url: url,
            beforeSend:
                function () {
                    document.getElementById('affichage-applications').innerHTML = "Récupération des applications de la fiche metier #" + fichemetier_id;
                },
            success:
                function (retour) {
                    document.getElementById('affichage-applications').innerHTML = retour;
                }
        });
    });
    $('select').selectpicker();


</script>