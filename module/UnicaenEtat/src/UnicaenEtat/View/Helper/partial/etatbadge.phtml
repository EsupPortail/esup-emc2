<?php

use UnicaenEtat\Entity\Db\Etat;

/**
 * @var Etat $etat
 * @var array $options
 */

?>

<?php if ($etat === null) : ?>
    <div class="label leftpart" title="État inconnu">
            <i class="fa fa-question-circle" aria-hidden="true"></i>
        </div>
    <div class="label rightpart" title="Type inconnu">
            <i class="fa fa-question-circle" aria-hidden="true"></i>
        </div>

    <style>
        .leftpart {
            background: #8e7c7c;
            position:relative;
            left:0.5rem;
            width:2.5rem;
            display: inline-block;
        }
        .rightpart {
            position:relative;
            left:-0.25rem;
            background: linear-gradient(90deg, transparent 2px, #7f8e7c 0);
            width:2.5rem;
            display: inline-block;
        }
    </style>
<?php else : ?>
    <div class="label leftpart<?php echo $etat->getId(); ?>" title="<?php echo $etat->getLibelle(); ?>">
            <i class="<?php echo ($etat->getIcone())?:"fa fa-question-circle"; ?>" aria-hidden="true"></i>
        </div>
    <div class="label rightpart<?php echo $etat->getType()->getId(); ?>" title="<?php echo $etat->getType()->getLibelle(); ?>">
            <i class="<?php echo ($etat->getType()->getIcone())?:"fa fa-question-circle"; ?>" aria-hidden="true"></i>
        </div>

    <style>
        .leftpart<?php echo $etat->getId(); ?> {
            background: <?php echo ($etat->getCouleur())?:"#8e7c7c"; ?>;
            position:relative;
            left:0.5rem;
            width:2.5rem;
            display: inline-block;
        }
        .rightpart<?php echo $etat->getType()->getId(); ?> {
            position:relative;
            left:-0.25rem;
            background: linear-gradient(90deg, transparent 2px, <?php echo ($etat->getType()->getCouleur())?:"#7f8e7c"; ?> 0);
            width:2.5rem;
            display: inline-block;
        }
    </style>
<?php endif; ?>