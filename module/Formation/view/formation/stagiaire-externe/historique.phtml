<?php

use Application\Entity\Db\Agent;
use Formation\Entity\Db\Inscription;
use Formation\Entity\Db\StagiaireExterne;

/**
 * @see \Formation\Controller\StagiaireExterneController::historiqueAction()
 * @var StagiaireExterne $stagiaire
 * @var Inscription[] $inscriptions
 */

$this->headTitle("Historique des formations de ". $stagiaire->getDenomination(true));


usort($inscriptions, function (Inscription $a, Inscription $b) { return $a->getSession()->getDebut(true) <=> $b->getSession()->getDebut(true);});
?>

<?php foreach ($inscriptions as $inscription) : ?>
    <?php echo $this->inscription($inscription); ?>
<?php endforeach; ?>
