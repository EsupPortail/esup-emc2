<?php

use UnicaenApp\Form\Element\SearchAndSelect;

/**
 * @see \Formation\Controller\SessionController::ajouterAvecFormulaireAction()
 */

?>

<form method="post" action="<?php echo $this->url('formation/session/ajouter-avec-formulaire', [], [], true); ?>">
    <label for="formation">Formation associée à la nouvelle session : </label>
    <?php
    /** @see  \Formation\Controller\FormationController::rechercherFormationsActivesAction() */
    $structureF = new SearchAndSelect('formation');
    $structureF
        ->setAutocompleteSource($this->url('formation/rechercher-formations-actives'))
        ->setSelectionRequired()
        ->setAttributes([
            'id' => 'formation',
            'placeholder' => "Sélectionner une formation",
        ]);
    ?>
    <?php echo $this->formControlGroup($structureF); ?>
    <input type="submit" value="Créer nouvelle session" class="btn btn-primary action">
</form>