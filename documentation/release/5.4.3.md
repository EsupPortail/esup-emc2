# Version 5.4.3 (XX/XX/2026)

## Changements 

[CORRECTION]
* L'action d'ajout de fiche de poste au format pdf pour les supérieur·es était conditionné avec le mauvais privilège.
* L'assertion vérifiant l'accès à la page Fiches de poste d'un agent ne fonctionnait pas correctement

## Changement de structure de la base de données

### Sans BddAdmin

Aucun

### Avec BddAdmin

Aucun

## Modification des données

```postgresql
delete from unicaen_parametre_parametre where code='TEMOIN_GRADE';
delete from unicaen_parametre_parametre where code='FILTRAGE_GRADE/CORPS/EMPLOI_TYPE';
```
Plus d'ajustement pour les paramètres dans le fichier [Modification des paramètres associés à la campagne](parametre_ep.sql).

Modification des paramètres décrivant la version
```postgresql
update unicaen_parametre_parametre set valeur='5.4.3' where code='VERSION';
update unicaen_parametre_parametre set valeur='XX/XX/2026' where code='RELEASE_DATE';
```

## Évolution des librairies

Aucun

```bash
composer update
```

## Évolution de la liste des privilèges

Aucun

## Évolution de la liste des paramètres

* Retrait du paramètre (associé aux entretiens professionnel) `TEMOIN_GRADE` qui a été remplacé par `TEMOIN_CORPS`.
* Suppression de paramètre `FILTRAGE_GRADE/CORPS/EMPLOI_TYPE` qui n'est plus utilisé issue d'une correction de dernière minute pour la campagne 2024/2025.

## Évolution des templates

Aucun