# Version 5.4.1 (30/04/2026)

## Changements 

[CORRECTION]
* Correction du comportement du menu ressources pour mieux correspondre aux privilèges associés

[AMÉLIORATION]
* Changement de libellé de paramètre pour améliorer la clarté
* Changement de la description de certains templates
* Changement de libellé des catégories "Gestion des catégories"


## Changement de structure de la base de données

### Sans BddAdmin

Aucun

### Avec BddAdmin

Aucun

## Modification des données

```
-- Changement de libellé de paramètres
update unicaen_parametre_parametre set libelle='Blocage de l''affichage du CREP aux agents si validation du responsable non réalisée' where code='CREP_BLOCAGE_VISUALISATION_AGENT_AVANT_VALIDATION'
update unicaen_parametre_parametre set libelle='Blocage de l''affichage du CREF aux agents si validation du responsable non réalisée' where code='CREF_BLOCAGE_VISUALISATION_AGENT_AVANT_VALIDATION'

-- Changement de la description
update unicaen_renderer_template set description='<p>Transmission des observations à l''autorité hiérarchique de l''agent.</p>' where code = 'ENTRETIEN_VALIDATION_2-PAS_D_OBSERVATION';

-- Changement du libellé de certaines catégories de privilèges
update unicaen_privilege_categorie set libelle='Gestion des catégories de paramètres' where code='parametrecategorie';
update unicaen_privilege_categorie set libelle='Gestion des catégories statutaires' where code='categorie';
update unicaen_privilege_categorie set libelle='Gestion des catégories de formulaire' where code='autoformcategorie';
update unicaen_privilege_categorie set libelle='Gestion des champs de formulaire' where code='autoformchamp';
```

Modification des paramètres décrivant la version
```postgresql
update unicaen_parametre_parametre set valeur='5.4.1' where code='VERSION';
update unicaen_parametre_parametre set valeur='30/04/2026' where code='RELEASE_DATE';
```

## Évolution des librairies

Aucun

## Évolution de la liste des privilèges

Aucun

## Évolution de la liste des paramètres

Les paramètres associés aux blocages des CREP et CREF avant validation ont reçu un nouveau libellé plus clair.

## Évolution des templates

La description du template `ENTRETIEN_VALIDATION_2-PAS_D_OBSERVATION` a été mis à jour pour éviter la confusion avec le template `ENTRETIEN_VALIDATION_2-OBSERVATION_TRANSMISSION`.
